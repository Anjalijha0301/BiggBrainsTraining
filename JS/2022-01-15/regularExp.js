let pera = "method searches a string for a specified value and returns the position of the match"

//search method 
let n = pera.search("specified");
console.log(n)

//replace method
let rep = pera.replace("specified", "W3Schools")
console.log(rep)